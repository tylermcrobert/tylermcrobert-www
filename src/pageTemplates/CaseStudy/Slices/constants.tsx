// eslint-disable-next-line no-unused-vars
import { FmType } from "util/formatImg"

export const HALF_SIZE = { w: 1200, fm: "webp" as FmType }
export const FULL_SIZE = { w: 1920, fm: "webp" as FmType }
