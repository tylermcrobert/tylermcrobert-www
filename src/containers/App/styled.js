import { sizes, media } from './styled/media';
import GlobalStyle from './styled/GlobalStyle';
import { durations, eases, transitions, poses } from './styled/anim';

export {
  sizes,
  media,
  durations,
  eases,
  transitions,
  poses,
};

export default GlobalStyle;
